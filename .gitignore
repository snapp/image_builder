# OS files
*~
._*
*.bak
*.bk
.bundle
.cache
.DS_Store
.history
*.old
*.orig
.settings
*.swp
Thumbs.db
.Trashes
.ansible

# Packer - local ISO storage (large binaries, never commit)
packer/isos/
packer/build*

# Packer - build outputs and manifests
packer/**/builds/

# Packer - user-specific variable files (filled-in copies of *.example)
packer/vsphere.pkrvars.hcl
